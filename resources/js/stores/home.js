import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const productStore = {
    namespaced: true,
    state: {
    },
    mutations: {
    },
    actions: {
    }
};

export default productStore;
